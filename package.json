{
  "name": "cypress-bdd-arquiteture",
  "version": "1.0.5",
  "description": "template using cypress with bdd architecture in Javascript",
  "main": "cypress.config.js",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "cy:run:headed": "cypress run --headed",
    "cy:run:chrome": "cypress run --browser chrome",
    "cy:run:firefox": "cypress run --browser firefox",
    "cy:clean": "rimraf reports screenshots videos",
    "cy:report": "npx multiple-cucumber-html-reporter --config cucumber-html-report.js",
    "test": "npm run cy:run"
  },
  "keywords": [
    "cypress",
    "automation",
    "bdd",
    "cucumber",
    "e2e-tests"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/WillianMPfeifer/Automation-E2E-Web-with-Cypress.git"
  },
  "author": "@WillianMPfeifer",
  "license": "MIT",

  "cypress-cucumber-preprocessor": {
      "nonGlobalStepDefinitions": false,
      "stepDefinitions": "cypress/support/steps/**/*.js",
      "messages": {
        "enabled": false
      },
      "json": {
        "enabled": false
      },
      "html": {
        "enabled": false
      }
    },

  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^23.2.1",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.8",
    "cypress": "^13.6.2",
    "esbuild": "^0.25.0",
    "multiple-cucumber-html-reporter": "^3.6.2",
    "rimraf": "^5.0.5"
  }
}
